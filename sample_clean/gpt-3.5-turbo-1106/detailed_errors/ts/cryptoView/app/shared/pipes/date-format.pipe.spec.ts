import { DateFormatPipe } from './date-format.pipe';

describe('DateFormatPipe', () => {
  it('should create a DateFormatPipe instance', () => {
    // Arrange
    const pipe = new DateFormatPipe();
    
    // Act
    
    // Assert
    expect(pipe).toBeTruthy();
  });
});